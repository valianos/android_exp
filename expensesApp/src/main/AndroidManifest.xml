<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.lianos.darn.expenses">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".activities.LoginActivity" android:label="@string/log_in"/>
        <activity android:name=".activities.PersonalInfoActivity" android:label="@string/personal_info"/>
        <activity android:name=".activities.SignUpActivity" android:label="@string/sign_up"/>
        <activity
                android:name=".activities.DisplayActivity"
                android:label="@string/title_activity_display"
                android:theme="@style/AppTheme.NoActionBar">
        </activity>
    </application>

</manifest>